const magicSeaWeed = {
    // fetch request with data
    // https://services.surfline.com/kbyg/spots/forecasts/{type}?{params}
    //https://services.surfline.com/kbyg/spots/forecasts/wave?{params}
    async getSpotData(type, spot) {};
}